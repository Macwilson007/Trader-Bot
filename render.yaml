services:
  - type: web
    name: trader-bot-bybit
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn traderbot.bot:app --bind 0.0.0.0:$PORT --log-level info
    envVars:
      - key: EXCHANGE
        value: BYBIT
      - key: BYBIT_TESTNET
        value: "false"
      - key: BYBIT_CATEGORY
        value: linear
      - key: BYBIT_API_KEY
        sync: false
      - key: BYBIT_API_SECRET
        sync: false
